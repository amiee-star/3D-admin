import { PageProps } from "@/interfaces/app.interface"
import React from "react"

const SalesDiscount = (props: PageProps) => {
	return <>SalesDiscount</>
}
SalesDiscount.title = "折扣等级"
export default SalesDiscount
