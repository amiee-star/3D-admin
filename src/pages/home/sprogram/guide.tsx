import { PageProps } from "@/interfaces/app.interface"
import React from "react"

const SprogramGuide = (props: PageProps) => {
	return <>SprogramGuide</>
}
SprogramGuide.title = "首页板块"
export default SprogramGuide
