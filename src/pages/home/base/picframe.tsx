import { PageProps } from "@/interfaces/app.interface"
import React from "react"

const BasePicFrame = (props: PageProps) => {
	return <>BasePicFrame</>
}
BasePicFrame.title = "画框管理"
export default BasePicFrame
