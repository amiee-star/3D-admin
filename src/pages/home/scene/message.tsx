import { PageProps } from "@/interfaces/app.interface"
import React from "react"

const SceneMessage = (props: PageProps) => {
	return <>SceneMessage</>
}
SceneMessage.title = "消息记录"
export default SceneMessage
