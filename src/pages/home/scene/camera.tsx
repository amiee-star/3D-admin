import { PageProps } from "@/interfaces/app.interface"
import React from "react"

const SceneCamera = (props: PageProps) => {
	return <>SceneCamera</>
}
SceneCamera.title = "场景相机"
export default SceneCamera
