import { PageProps } from "@/interfaces/app.interface"
import React from "react"

const BaseMaterial = (props: PageProps) => {
	return <>BaseMaterial</>
}
BaseMaterial.title = "公共素材"
export default BaseMaterial
