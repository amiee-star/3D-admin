import { PageProps } from "@/interfaces/app.interface"
import React from "react"

const SceneMaterial = (props: PageProps) => {
	return <>SceneMaterial</>
}
SceneMaterial.title = "素材管理"
export default SceneMaterial
