import { PageProps } from "@/interfaces/app.interface"
import React from "react"

const SprogramCard = (props: PageProps) => {
	return <>SprogramCard</>
}
SprogramCard.title = "首页名片"
export default SprogramCard
