import { PageProps } from "@/interfaces/app.interface"
import React from "react"

const SprogramActivity = (props: PageProps) => {
	return <>SprogramActivity</>
}
SprogramActivity.title = "活动管理"
export default SprogramActivity
