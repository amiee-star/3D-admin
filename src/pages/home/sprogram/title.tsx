import { PageProps } from "@/interfaces/app.interface"
import React from "react"

const SprogramTitle = (props: PageProps) => {
	return <>SprogramTitle</>
}
SprogramTitle.title = "首页标题"
export default SprogramTitle
