import { PageProps } from "@/interfaces/app.interface"
import React from "react"

const ManageTask = (props: PageProps) => {
	return <>ManageTask</>
}
ManageTask.title = "计划任务"
export default ManageTask
