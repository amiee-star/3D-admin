import { PageProps } from "@/interfaces/app.interface"
import React from "react"

const SalesStatistics = (props: PageProps) => {
	return <>SalesStatistics</>
}
SalesStatistics.title = "统计"
export default SalesStatistics
