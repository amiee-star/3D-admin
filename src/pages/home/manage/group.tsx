import { PageProps } from "@/interfaces/app.interface"
import React from "react"

const ManageGroup = (props: PageProps) => {
	return <>ManageGroup</>
}
ManageGroup.title = "访问组"
export default ManageGroup
