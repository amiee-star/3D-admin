import { PageProps } from "@/interfaces/app.interface"
import React from "react"

const SalesPerson = (props: PageProps) => {
	return <>SalesPerson</>
}
SalesPerson.title = "销售员"
export default SalesPerson
